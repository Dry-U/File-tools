{
  "name": "filetools",
  "version": "1.1.0",
  "description": "智能文件检索与问答系统 - Tauri Desktop App",
  "scripts": {
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "build:backend": "python build_pyinstaller.py",
    "build:all": "npm run build:backend && npm run tauri:build",
    "dev": "python main.py",
    "start:all": "npm run dev & sleep 3 && npm run tauri:dev"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1"
  }
}
